.DS_Store
.venv/
__pycache__/
.claude/settings.local.json

# Код доступа: читается tools/build.sh, в репозиторий не попадает.
.env.local

# Именная версия: полные фамилии, без шифрования. Живёт только в папке Свод
# на Диске. Строка здесь - страховка на случай, если файл окажется рядом.
Отчёт_именной_2В.html
